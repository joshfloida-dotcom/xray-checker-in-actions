name: Run xray-checker (Safe & Optimized)

on:
  workflow_dispatch:
    inputs:
      args:
        description: "Аргументы для ручного запуска"
        required: false
        default: ""
  schedule:
    - cron: "0 */2 * * *" # Запуск каждые 2 часа

jobs:
  run-xray-checker:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      # Шаг 1: Агрегация, декодирование и глубочайшая валидация (Go-Strict)
      - name: Aggregate and Validate
        run: |
          python3 - << 'EOF'
          import urllib.request
          import urllib.parse
          import re
          import random
          import base64
          import json

          SOURCES = [
              "https://raw.githubusercontent.com/capitainblack/freetm3/refs/heads/main/configs/sub_1.txt",
              "https://raw.githubusercontent.com/capitainblack/freetm3/refs/heads/main/configs/sub_2.txt",
              "https://raw.githubusercontent.com/capitainblack/freetm3/refs/heads/main/configs/sub_3.txt",
              "https://raw.githubusercontent.com/capitainblack/freetm3/refs/heads/main/configs/sub_4.txt",
              "https://raw.githubusercontent.com/capitainblack/freetm3/refs/heads/main/configs/sub_5.txt",
              "https://raw.githubusercontent.com/capitainblack/freetm3/refs/heads/main/configs/sub_6.txt",
              "https://raw.githubusercontent.com/capitainblack/freetm3/refs/heads/main/configs/sub_7.txt",
              "https://raw.githubusercontent.com/capitainblack/freetm3/refs/heads/main/configs/sub_8.txt",
              "https://raw.githubusercontent.com/capitainblack/freetm3/refs/heads/main/configs/sub_9.txt",
              "https://raw.githubusercontent.com/capitainblack/freetm3/refs/heads/main/configs/sub_10.txt"
          ]

          def decode_subscription(content):
              content = content.strip()
              if "://" not in content:
                  try:
                      missing_padding = len(content) % 4
                      if missing_padding: content += '=' * (4 - missing_padding)
                      return base64.b64decode(content).decode('utf-8', errors='ignore')
                  except:
                      pass
              return content

          def is_valid_vmess(link):
              try:
                  payload = link[8:].split('#')[0]
                  # Нормализация Base64
                  payload_std = payload.replace('-', '+').replace('_', '/')
                  payload_std += "=" * ((4 - len(payload_std) % 4) % 4)
                  
                  # Строгая проверка Base64 (как в Go)
                  try:
                      decoded_bytes = base64.b64decode(payload_std, validate=True)
                  except:
                      return False
                  
                  data = json.loads(decoded_bytes.decode('utf-8'))
                  
                  # Xray удалил поддержку tcp+http
                  if data.get('net') == 'tcp' and data.get('type') == 'http':
                      return False
                      
                  # Защита от ошибки "cannot unmarshal bool"
                  if isinstance(data.get('tls'), bool):
                      return False
                      
                  return "add" in data or "id" in data
              except:
                  return False

          def is_valid_vless_trojan(link):
              try:
                  parsed = urllib.parse.urlparse(link)
                  # Обязательно должен быть ID (для Vless) или Пароль (для Trojan)
                  if not parsed.username:
                      return False
                      
                  qs = urllib.parse.parse_qs(parsed.query)
                  
                  # Xray удалил поддержку tcp+http
                  if qs.get('type', [''])[0] == 'tcp' and qs.get('headerType', [''])[0] == 'http':
                      return False
                      
                  # Проверка структуры REALITY
                  if qs.get('security', [''])[0] == 'reality':
                      if not qs.get('pbk'):
                          return False
                          
                  return True
              except:
                  return False

          def is_clean_link(link):
              if not link or len(link) < 15:
                  return False
              payload = link.split('#')[0]
              # Пропускаем только чистый ASCII, чтобы не крашить Go-парсер
              if not all(ord(c) < 128 for c in payload):
                  return False
              return True

          unique_configs = set()
          total_found = 0

          print("[*] Скачивание и фильтрация конфигов...")

          for url in SOURCES:
              try:
                  req = urllib.request.Request(url, headers={'User-Agent': 'Mozilla/5.0'})
                  with urllib.request.urlopen(req, timeout=15) as response:
                      raw_content = response.read().decode('utf-8', errors='ignore')
                      content = decode_subscription(raw_content)
                      
                      matches = re.findall(r'(?:vmess|vless|ss|trojan)://[^\s"' + "'" + r'<>]+', content)
                      total_found += len(matches)

                      for match in matches:
                          match = match.strip()
                          if not is_clean_link(match):
                              continue
                          
                          if match.startswith("vmess://"):
                              if is_valid_vmess(match):
                                  unique_configs.add(match)
                          elif match.startswith("vless://") or match.startswith("trojan://"):
                              if is_valid_vless_trojan(match):
                                  unique_configs.add(match)
                          # ss:// (Shadowsocks) пропускаем намеренно - они вызывают панику ядра
                          
              except Exception as e:
                  print(f"[!] Ошибка загрузки {url}: {e}")

          unique_list = list(unique_configs)
          random.shuffle(unique_list)
          
          CHECK_LIMIT = 2000
          selected_configs = unique_list[:CHECK_LIMIT]
          
          print(f"[*] Всего ссылок найдено до фильтрации: {total_found}")
          print(f"[+] Успешно очищено и отобрано: {len(selected_configs)}")

          with open("clean_proxies.txt", "w", encoding="utf-8") as f:
              for config in selected_configs:
                  if config.strip():
                      f.write(config.strip() + "\n")
          EOF

      # Шаг 2: Установка чекера
      - name: Install Xray Checker
        run: |
          curl -sL -o - $(curl -s https://api.github.com/repos/kutovoys/xray-checker/releases/latest | grep "browser_download_url.*linux-amd64.tar.gz" | cut -d'"' -f4) | tar -xz
          chmod +x xray-checker

      # Шаг 3: Запуск проверки и вывод логов в файл
      - name: Execute Check
        env:
          SUBSCRIPTION_URL: "file://${{ github.workspace }}/clean_proxies.txt"
          METRICS_PUSH_URL: ${{ secrets.PUSH_URL }}
          INSTANCE: ${{ secrets.INSTANCE || 'github-actions-sampled' }}
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ] && [ -n "${{ github.event.inputs.args }}" ]; then
            ./xray-checker ${{ github.event.inputs.args }} | tee xray_check_log.txt
          else
            ./xray-checker \
              --subscription-url="$SUBSCRIPTION_URL" \
              --metrics-push-url="$METRICS_PUSH_URL" \
              --metrics-instance="$INSTANCE" \
              --run-once | tee xray_check_log.txt || echo "Проверка завершена"
          fi

      # Шаг 4: Сохранение артефактов
      - name: Upload Scanned Configs Artifact
        uses: actions/upload-artifact@v4
        with:
          name: Scanned-Proxies-and-Logs
          path: |
            clean_proxies.txt
            xray_check_log.txt
          retention-days: 5
